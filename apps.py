from django.apps import AppConfig

class ErrorTrackingConfig(AppConfig):
    name = 'superapp.apps.error_tracking'
    default_auto_field = 'django.db.models.BigAutoField'
    verbose_name = 'Error Tracking'

